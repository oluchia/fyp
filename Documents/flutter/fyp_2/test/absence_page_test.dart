import 'package:flutter_test/flutter_test.dart';
import 'package:fyp/pages/absence_page.dart';
import 'package:fyp/models/absence_form.dart';
import 'package:flutter/material.dart';
import 'package:fyp/services/authentication.dart';


void main() {
  testWidgets('testing enter text in form', (WidgetTester tester) async {
   // await tester.pumpWidget(AbsencePage(auth: new Auth()));

    //await tester.enterText(find.byType(TextFormField), 'hi');
  });
}